{
  "name": "restrict-scroll",
  "version": "1.3.7",
  "description": "A small library to prevent scrolling on every element unless otherwise allowed.",
  "homepage": "https://github.com/nullset/restrict-scroll",
  "main": "src/index.js",
  "keywords": [
    "scroll"
  ],
  "files": [
    "src/index.js"
  ],
  "author": "Nathan Wright",
  "license": "ISC",
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\""
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "snowpack": "^2.18.2"
  },
  "dependencies": {
    "normalize-wheel": "^1.0.1",
    "on-paint": "^0.5.4"
  }
}
